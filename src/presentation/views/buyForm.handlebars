<!DOCTYPE html>
<html>
<head>
    <title>Formulario de Pago</title>
</head>
<body>
    <h1>Formulario de Pago con Tarjeta</h1>
    <form id="payment-form">
        <label for="card-number">Número de Tarjeta:</label>
        <input type="text" id="card-number" name="cardNumber" placeholder="Número de tarjeta" required>
        <br>
        <label for="card-name">Nombre en la Tarjeta:</label>
        <input type="text" id="card-name" name="cardName" placeholder="Nombre en la tarjeta" required>
        <br>
        <label for="expiry">Fecha de Vencimiento:</label>
        <input type="text" id="expiry" name="expiry" placeholder="MM/AA" required>
        <br>
        <label for="cvv">CVV:</label>
        <input type="text" id="cvv" name="cvv" placeholder="CVV" required>
        <br>
        <button type="submit">Realizar Pago</button>
    </form>

    <script id="payment-form-template" type="text/x-handlebars-template">
        <!-- Inserta aquí tu código Handlebars -->
    </script>
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.7/handlebars.min.js"></script>
    <script>
        const source = document.getElementById("payment-form-template").innerHTML;
        const template = Handlebars.compile(source);
        const context = {}; // Puedes agregar datos contextuales aquí si es necesario
        const html = template(context);
        document.getElementById("payment-form").innerHTML = html;
    </script>
</body>
</html>
